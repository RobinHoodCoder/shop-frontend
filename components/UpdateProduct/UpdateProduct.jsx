import React from 'react';

const UpdateProduct = (props) => {
  const { dummy } = props;
  // Get existing

  // get mutation

  // handle updates in forms

  return (
    <div>

    </div>
  );
};

export default UpdateProduct;
