import styled from 'styled-components';

const Description = styled.p`
  text-align: center;
  a {
    background: var(--red);
    display: inline;
    line-height: 1.2;
    font-size: 1rem;
    text-align: center;
    color: white;
    padding: 0 1rem;
  }
`;

export default Description;
