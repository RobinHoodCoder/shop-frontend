import styled from 'styled-components';

const Description = styled.p`
  span {
    line-height: 1.2;
    font-size: 1.3rem;
    color: --var(black);
    padding: 0 1rem;
  }
`;

export default Description;
