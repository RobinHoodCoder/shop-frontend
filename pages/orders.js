import React from 'react';
import { Link } from '../consts/exports';

const Orders = (props) => {
  const { dummy } = props;

  return (
    <div>
      <Link href={''}>oke</Link>

    </div>
  );
};

export default Orders;
